export class Socket {
    links = [];

    constructor(
        _valueTypeName,
        _name,
        _value = undefined,
        _label = undefined,
        _valueChoices, // if not empty, value must be one of these.
    ) {}
}
