export class Socket {
    links = [];

    constructor(
        valueTypeName,
        name,
        value = undefined,
        label = undefined,
        valueChoices, // if not empty, value must be one of these.
    ) {}
}
